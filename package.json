{
	"name": "evil-bob",
	"workspaces": ["packages/*"],
	"devDependencies": {
		"@biomejs/biome": "^1.9.4"
	},
	"scripts": {
		"lint": "biome check --write . && tsc",
		"dev:chromium": "cd packages/extension && bun run dev:chromium",
		"build:chromium": "cd packages/extension && bun run build:chromium",
		"build": "cd packages/extension && bun run build",
		"build:plugins": "cd packages/plugins && bun run build"
	}
}
